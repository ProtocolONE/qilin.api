package middleware

import (
	"github.com/labstack/echo"
	"testing"
)

func testAccess(t *testing.T, path string, vendor string, role string, owner string, res, id string, result bool) {
	echo.New()
	//CREATE USER
	//Add ROLES
	//Echo call
	//
}